%h1 Products
%table.table.table-striped
  %thead
    %tr
      %th ID
      %th Name
      %th Created at
  %tbody
    - @products.each do |product|
      %tr
        %td= product.id
        %td= link_to product.title, "https://#{@shop_session.url}/admin/products/#{product.id}", target: "_top"
        %td= product.created_at
